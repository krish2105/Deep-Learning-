# Build-time API URL for production. NOT a secret — it is a public endpoint, and
# NEXT_PUBLIC_* values are inlined into the client bundle regardless.
# Committed so the Vercel build is reproducible without dashboard state.
# Change this to your Render URL if you deploy the API elsewhere.
NEXT_PUBLIC_API_URL=https://sentinel-cxr-api.onrender.com
