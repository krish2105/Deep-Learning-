# Inference core — Hugging Face Spaces free CPU (2 vCPU, 16 GB RAM).
# Memory is plentiful here, so full PyTorch is fine. This is exactly what
# does NOT fit in Render's 512 MB, which is why the system is split in two.

--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.5.1
torchvision==0.20.1
torchxrayvision==1.3.4     # pretrained ChestX-ray14 DenseNet-121 (Cohen et al.)

fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pillow==11.1.0
numpy==2.2.1
scikit-image==0.25.0       # torchxrayvision preprocessing dependency
